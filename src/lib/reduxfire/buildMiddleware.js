// import {allRemotes, REMOTE_WATCH} from './types'

export default function() {
//   return store => next => action => {
//     return next(action)
    // if (allRemotes.includes(action.type)) {
    //   return new Promise( (resolve)=>{
    //     switch (action.type) {
    //       case REMOTE_WATCH:
    //         // do a thing, then resolve
    //         ref.once('value')
    //         break;
    //     }
    //   })
    // } else {
    //   next(action)
    // }
  // }
}